info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/v1/auth/login
  body:
    type: json
    data: |-
      {
        "email": "he@gmail.com",
        "password": "hee@gmail12"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
