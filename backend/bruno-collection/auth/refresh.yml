info:
  name: refresh
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3000/api/v1/auth/refresh
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
